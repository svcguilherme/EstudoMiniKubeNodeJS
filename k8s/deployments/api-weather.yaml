apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-weather
  namespace: temperaturaapp
  labels:
    app: api-weather
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-weather
  template:
    metadata:
      labels:
        app: api-weather
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port:   "3001"
        prometheus.io/path:   "/metrics"
    spec:
      containers:
        - name: api-weather
          image: api-weather:local
          imagePullPolicy: Never
          ports:
            - containerPort: 3001
          env:
            - name: PORT
              value: "3001"
            - name: OPENWEATHER_API_KEY
              valueFrom:
                secretKeyRef:
                  name: api-keys
                  key: OPENWEATHER_API_KEY
            - name: RABBITMQ_URL
              value: amqp://rabbitmq:5672
            - name: MONGODB_URI
              valueFrom:
                secretKeyRef:
                  name: api-keys
                  key: MONGODB_URI
            - name: REDIS_URL
              value: redis://redis:6379
            - name: SQLITE_PATH
              value: /data/api-weather.db
            - name: OTEL_SERVICE_NAME
              value: api-weather
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: http://otel-collector:4318
          volumeMounts:
            - name: sqlite-data
              mountPath: /data
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 256Mi
              cpu: 300m
      volumes:
        - name: sqlite-data
          persistentVolumeClaim:
            claimName: sqlite-weather-pvc
